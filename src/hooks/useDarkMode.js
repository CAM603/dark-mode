import { useEffect } from 'react';
import { useLocalStorage } from './useLocalStorage';

export const useDarkMode = () => {

}

const [someValue, setSomevalue] = useLocalStorage('a key');

useEffect(() => {

}, [])